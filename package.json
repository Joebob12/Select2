{
  "name": "@joenied/select2",
  "version": "1.0.5",
  "description": "",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/Joebob12/Select2.git"
  },
  "keywords": [
    "select"
  ],
  "main": "dist/index.js",
  "module": "dist/index.js",
  "babel": {
    "presets": [
      "@babel/react"
    ],
    "plugins": [
      "transform-class-properties"
    ]
  },
  "dependencies": {
    "react": "16.8.6",
    "react-dom": "16.8.6",
    "react-scripts": "3.0.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "babel-plugin-transform-class-properties": "^6.24.1",
    "typescript": "3.3.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "publish:npm": "set NODE_ENV=production && rm -rf dist && mkdir dist && npx babel src/select --out-dir dist --copy-files"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
